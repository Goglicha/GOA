def summation(nums):
    first_number = 1
    sum_of_numbers = 0
    while first_number <= nums:
        sum_of_numbers = first_number + (first_number+1)
    return sum_of_numbers

print(summation([5]))
        
        
    
        
        
    
